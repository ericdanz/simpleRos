#include <ros.h>
#include <AFMotor.h>
#include <geometry_msgs/Twist.h>


ros::NodeHandle nh;

std_msgs::String boot_msg;
ros::Publisher bootmsg('boot', &str_msg);

void runMotors( const geometry_msgs::Twist& input_msg){
   digitalWrite(13, HIGH-digitalRead(13)); 
}

void requests(

ros::Subscriber<geometry_msgs::Twist> sub("inputs", &runMotors );
ros::Subscriber<std_msgs::String> sub("reqs", &requests );
